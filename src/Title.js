import React from 'react';

const Title = () => {
    return (
        <div className="titleApp">
           <span className="bold">PURE</span>
           <span>JS</span>
           <span className="bold">DATE-PICKER</span>
        </div>
    );
};

export default Title;